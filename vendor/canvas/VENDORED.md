Real-file copies of the `paulomtts.canvas` plugin's component files
(Canvas, CanvasNode, CanvasEdges, CanvasControls, camera.js, layout.js,
positions.js). Omarchy plugins cannot import from each other and symlinks fail
plugin validation, so the component is vendored. Re-copy from that plugin to
update; do not edit these files here.
